{
  "name": "c_vis",
  "version": "0.2.0",
  "private": true,
  "description": "Visual execution for C programs, powered by GDB.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "start": "node server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.1.0",
    "svelte": "^5.38.0",
    "vite": "^7.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
